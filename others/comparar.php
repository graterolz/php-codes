<?php
	// define variables
	$p = 10;
	$q = 11;
	$r = 11.3;
	$s = 11;
	// prueba si $q es mayor que $p
	// regresa el valor verdadero (true)
	echo ($q > $p);
	// prueba si $q es menor que $p
	// regresa el valor de falso (false)
	echo ($q < $p);
	// prueba si $q es mayor o igual a $s
	// regresa el valor de verdadero (true)
	echo ($q >= $s);
	// prueba si $r es menor o igual a $s
	// regresa el valor de falso (false)
	echo ($r <= $s);
	// prueba si $q es igual a $s
	// regresa el valor de verdadero (true)
	echo ($q == $s);
	// prueba si $q es igual a $r
	// regresa el valor de falso (false)
	echo ($q == $r);
?>